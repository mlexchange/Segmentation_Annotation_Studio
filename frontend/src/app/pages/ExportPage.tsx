/** Export page — COCO dataset export + import. */
export default function ExportPage() {
  return (
    <div className="flex h-full items-center justify-center text-gray-400">
      <p>Export — COCO dataset writer coming soon</p>
    </div>
  );
}
