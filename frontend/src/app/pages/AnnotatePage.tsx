/** Annotate page — react-konva canvas workspace. */
export default function AnnotatePage() {
  return (
    <div className="flex h-full items-center justify-center text-gray-400">
      <p>Annotate — canvas workspace coming soon</p>
    </div>
  );
}
