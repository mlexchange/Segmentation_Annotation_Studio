/** Preprocess Hub stage — features, Suggest Labels, placement mask. */
import AnnotateWorkspace from './AnnotateWorkspace';

export default function PreprocessPage() {
  return <AnnotateWorkspace />;
}
