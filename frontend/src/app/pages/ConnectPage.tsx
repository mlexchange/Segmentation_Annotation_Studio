/** Connect page — dataset picker (Tiled + local folder). */
export default function ConnectPage() {
  return (
    <div className="flex h-full items-center justify-center text-gray-400">
      <p>Connect — dataset picker coming soon</p>
    </div>
  );
}
